#include<iostream>
#include<queue>
using namespace std;
int main()
{
    priority_queue<int>q;
    q.push(90);
    q.push(10);
    q.push(70);
    q.push(50);
    q.push(100);

    while(!q.empty())
    {
        int x;
        x=q.top();
        cout << x << endl;
       q.pop();

    }
}
